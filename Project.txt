EC2 Ubuntu using .ppk:
Security Groups → Inbound Rules → SSH → Port 22 → My IP
PuTTY → Session → ubuntu@Public-IP/DNS
PuTTY → Connection → SSH → Auth → Credentials → Browse .ppk
Open → Yes(Security Alert)-> whoami

EC2 Ubuntu using PEM:
PuTTYgen → Load → Select .pem → Open → Save private key → Save .ppk
EC2 → Instances → Copy Public DNS/IP
PuTTY → Session → ubuntu@Public-IP/DNS
Connection → SSH → Auth → Credentials → Browse .ppk
Open → Yes-whoami

Windows EC2 using RDP
EC2 → Launch Instance → Windows Server AMI
Security Group → RDP → Port 3389 → My IP
Create/Select Key Pair → Download .pem
EC2 → Instance → Copy Public IP/DNS
Instance → Connect → RDP Client → Get Password
Upload .pem → Decrypt Password → Copy Password
Open mstsc(remote desktop connection)
Computer → Public IP/DNS
Username:Administrator
Password → Paste decrypted password
Connect → OK
✔ Windows Desktop Opens
-----------------------------------------------------------------------------------
EBS:
(a). Create & Attach EBS Volume
EC2 → Volumes → Create Volume → gp3/gp2 → Size → Same AZ → Create
Volumes → Select Volume → Actions → Attach Volume → Select EC2 → /dev/xvdf → Attach
SSH into EC2 →
lsblk
1(b). Increase CPU & RAM (Change Instance Type)
EC2 → Instances → Stop Instance
Actions → Instance Settings → Change Instance Type
Select higher type (t2.micro → t2.small etc.)
Apply → Start Instance

2. Permanently Mount EBS Volume
EC2 → Volumes → Create Volume → gp3 → 20GB → Same AZ
Volumes → Attach Volume → Select EC2 → /dev/xvdf
SSH into EC2:
lsblk -fs
fdisk /dev/nvme1n1
n → p → 1 → Enter → Enter → w
lsblk -fs
mkfs.xfs /dev/nvme1n1p1
mkdir /mnt/archana
mount /dev/nvme1n1p1 /mnt/archana
df -h
blkid /dev/nvme1n1p1
nano /etc/fstab
UUID=<uuid> /mnt/archana xfs defaults,nofail 0 0
mount -a
cd /mnt/archana ->chmod 400 /mnt/archana-> nano file.txt
reboot
df -h

Detach & Reattach Volume
Volumes → Select Volume → Actions → Detach Volume
Attach to another EC2 (same AZ)
SSH →
mkdir /mnt/archana
mount /dev/nvme1n1p1 /mnt/archana

3. Snapshot & Attach in Another Region
Volumes → Select Volume → Actions → Create Snapshot
Snapshots → Select Snapshot → Actions → Copy Snapshot
Choose Destination Region → Copy
Switch Region (top-right)
Snapshots → Select Copied Snapshot → Create Volume
Choose:Size,Type,AZ
Create Volume
Volumes → Select New Volume → Attach Volume
Select EC2 → Device Name → Attach
--------------------------------------------------------------------------
EFS:
Step 1: Launch EC2 Instances
EC2 → Launch Instance → Name:EFS-1
AMI → Amazon Linux, Type → t2.micro, Key Pair → EFS
Network → Subnet-1a
Security Group → New SG → Allow NFS
Launch
Repeat same:
EFS-2 → Subnet-1c

Step 2: Create EFS File System
EFS → Create File System
VPC → Default
Enable Region
Next →
Delete default SGs
Select NFS Security Group
Next → Create File System

Step 3: Access Both EC2 Instances
Open 2 PowerShell windows
SSH: ssh -i key.pem ec2-user@public-ip
sudo su
mkdir efs
yum install -y amazon-efs-utils
List files: ls
Repeat same for EFS-2

Step 4: Attach EFS to EC2
EFS → Select File System → Attach
Mount via DNS
Paste in BOTH EC2 terminals

Step 5: Verify EFS
cd efs
Create file in Instance-1: touch f1
Check in Instance-2: ls
----------------------------------------------------------------
S3 Public Bucket & Object Access
S3 → Create Bucket → Enter Bucket Name → Select Region
Block Public Access → Uncheck ALL → Acknowledge → Create Bucket
Bucket → Permissions → ACL → Edit
Everyone(public access) → ✔ Read → Save
Objects → Upload → Add files → Upload
Object → Permissions → ACL → Edit
Everyone(public access) → ✔ Read → Save
Copy Object URL → Open in Browser
✔ Public access working

S3 Versioning
S3 → Select Bucket → Properties
Bucket Versioning → Edit → Enable → Save
Upload report.pdf
✔ Version ID created
Upload same report.pdf again
✔ Old version preserved → New version current
Delete Object
✔ Delete Marker created → Old versions still available

Cross Region Replication (CRR)
Create Destination Bucket → Different Region → Create
Destination Bucket → Properties → Versioning → Enable
Source Bucket → Properties → Versioning → Enable
Source Bucket → Management → Replication Rules → Create Rule
Rule Name → academy-crr-rule
Status → Enabled
Scope → Apply to all objects
Destination → Another Bucket → Select Destination Bucket
Use AWS Managed Role → Default
Replicate → New Objects
Save Rule
Upload file in Source Bucket
Destination Bucket → Check Objects
✔ File replicated automatically

Static Website Hosting in S3
S3 → Create Bucket → Enter Name → Select Region
Block Public Access → Uncheck ALL → Acknowledge → Create Bucket
Bucket → Objects → Upload
Add:
index.html
error.html
Upload
Bucket → Properties → Static Website Hosting → Edit
Enable → Host Static Website
Index Document → index.html
Error Document → error.html
Save Changes
Bucket → Permissions → ACL → Edit
Everyone(public access) → ✔ Read → Save
Objects → index.html → Permissions → ACL → Edit
Everyone(public access) → ✔ Read → Save
Repeat same for error.html
Properties → Static Website Hosting
Copy Bucket Website Endpoint
Paste in Browser
✔ Website loads successfully
Invalid URL → bucket-url/invalid.html
✔ error.html page displayed
--------------------------------------------------------------------------------
VPC Creation & EC2 Connection:
VPC → Create VPC → VPC Only
Name:Custom-VPC
CIDR:10.0.0.0/16
Create VPC

Subnets → Create Subnet → Select VPC
Public Subnet:10.0.1.0/24
Private Subnet:10.0.2.0/24
Select AZ → Create

Internet Gateways → Create IGW
Name:Custom-IGW
Attach to VPC
Route Tables → Create Route Table → Select VPC
Public Route Table → Add Route
Destination:0.0.0.0/0
Target:Internet Gateway

Associate → Public Subnet
Private Route Table → Keep Local Route Only
Associate → Private Subnet
Public Subnet → Edit Subnet Settings

Enable:
✔ Auto-assign Public IPv4

Security Groups → Create SG
Inbound Rules: HTTP → 80 HTTPS → 443 SSH → 22

Attach SG to EC2
Launch EC2
Public EC2 → Public Subnet
Private EC2 → Private Subnet
Attach Security Groups
---------------------------------------------------------------
VPC using Bastion Server:

VPC CIDR → 10.0.0.0/16
Public Subnet(Bastion) → 10.0.1.0/24
Private Subnet(DB Server) → 10.0.2.0/24

Security Groups
Bastion SG:
SSH(22) → My IP
DB Server SG:
MySQL(3306) → Custom
SSH(22) → Bastion Subnet 10.0.1.0/24

EC2 Creation
Bastion Server:
Public Subnet
Ubuntu
bastion.pem

DB Server:
Private Subnet
Ubuntu
dbserver.pem
Copy DB Key to Bastion

PowerShell:
scp -i bastion.pem dbserver.pem ubuntu@<BASTION_PUBLIC_IP>:/home/ubuntu/

✔ dbserver.pem copied to Bastion

Connect to Bastion
ssh -i bastion.pem ubuntu@<BASTION_PUBLIC_IP>

Check files:ls
✔ dbserver.pem

Connect Bastion → DB Server
ssh -i dbserver.pem ubuntu@10.0.2.x

✔ Connected to Private DB Server

NAT Gateway Setup
VPC → NAT Gateway → Create NAT Gateway
Select:
Public Subnet
Elastic IP

Create
Route Tables → Create Route Table
Name:bastion-route
Select VPC → Create
Route Table → Edit Subnet Associations
Associate → Private Subnet
Route Table → Edit Routes
Add:
Destination:0.0.0.0/0
Target:NAT Gateway
Save

Private EC2:
apt update
or
yum update

✔ Internet working in Private Subnet
---------------------------------------------------------------------
