SQL Injection using Burp Suite in OWASP Juice Shop:
sudo apt update
sudo apt install docker.io –y
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
reboot
docker run hello-world
docker pull bkimminich/juice-shop
docker run -d -p 3000:3000 bkimminich/juice-shop
http://localhost:3000
Burpsuite
In Burp browser, open:
https://juice-shop.herokuapp.com/#/login
OR (local):
http://localhost:3000/#/login
Enter any random values:
Email: admin
Password: password
Click Login

Now go to Burp: Click HTTP History
Look for: /rest/user/login
Right-click on /rest/user/login -> Click: Send to Intruder
Go to: Intruder tab
Click Clear §
• Select email value:
Example:
"email":"admin"
• Click Add §
Now it becomes:
"email":"§admin§"

Go to:
Payloads tab
Open the website: https://github.com/Ninja-Yubaraj/SQL-Injection-Payloads-List
copy and paste from: Generic SQL Injection Payloads Section
After unchecking URL encoder
Now paste SQL injection payloads.
Example Payload List:
' OR 1=1 --
' OR '1'='1
admin' --
' OR 1=1#
' OR ''='
These payloads will be tested automatically
Uncheck:
URL Encode these characters

Click:
Start Attack You will see many responses.
Focus on:
• Status Code = 200
✅ (Success)
• Other codes = Failed
If 200 appears → SQL Injection worked

Click the 200 response
Go to:
Response tab
Look for:
authentication token
Copy it


Go to:
Decoder tab
1. Paste token
2. Select:
Decode as → Base64

 Use online MD5 decoder

After decoding, you will see:
email: admin@juice-sh.op
password: admin123
These are extracted credentials

Go back to Juice Shop:
Enter:
• Email
• Password (decoded one)
Click Login
Successfully logged in


' OR 1=1 --
' AND 1=1 --
' AND 1=2 –

-----------------------------------------------------
Cross-Site Scripting (XSS) using Juice Shop and Burp Suite:

docker pull bkimminich/juice-shop
docker run -d -p 3000:3000 bkimminich/juice-shop
http://localhost:3000
Open your browser and configure it to use Burp Suite as a proxy:
O Chrome: Settings > Proxy settings > Manual proxy > Set HTTP Proxy to
127.0.0.1, Port 8081.
O Firefox: Preferences > Network Settings > Manual proxy > Set HTTP Proxy to
127.0.0.1, Port 8081.

Browse through different sections of Juice Shop and look for input fields (such as search
bars, login forms, and feedback forms).
Proxy > HTTP history.
Locate the request corresponding to an input field and send it to Repeater (Right-
click > Send to Repeater).

In the Repeater tab, experiment by modifying the input fields with JavaScript payloads
such as:
<script>alert("XSS')</script>
Click Send

In Juice Shop, navigate to Contact Us or Feedback page. comment field: <script>alert("XSS')</script>
Submit the feedback form.

 If the site is vulnerable, an alert box will pop up.
5. Confirm the XSS attack by checking:
O The browser console (F12> Console).
O The HTTP history tab in Burp Suite.
Inspecting the request and response in Burp Suite.

Stored XSS Attack
1. Locate a form where the input is stored and displayed later (e.g., user profile, reviews, or
feedback section).
2. Enter the payload:
<script>document.write('<img src=x onerror=alert("Stored XSS")>");</script>
3. Submit the form.
4. Reload the page where the stored input is displayed.
---------------------------------------------------------------------------------------------
Testing Authentication Weaknesses and Session Management using Juice Shop :

sudo apt update 
sudo apt install docker.io -y 
sudo systemctl start docker 
sudo systemctl enable docker
sudo docker run -d -p 3000:3000 --name juice-shop bkimminich/juice-shop 
http://localhost:3000 
Open Burp Suite 
Proxy → Intercept ON
Set browser proxy: o IP: 127.0.0.1 o Port: 8080 
Test 1: SQL Injection Login Bypass
Open Login page ->Enter: Email: ' OR 1=1 -- Password: anything 

Test 2: Weak Password Policy
Register new account -> Use password: 12345 
Test 3: Brute Force Attack
Capture login request in Burp-> /rest/saveLoginIp-> Send to Intruder->Select password field -> $Add$

Load wordlist: /usr/share/wordlists/rockyou.txt 
Start attack 
Test 4: Username Enumeration
Try valid email + wrong password 
Try invalid email + wrong password 
PART B: SESSION MANAGEMENT TESTING
Test 5: Session Cookie Analysis 
Login -> Press F12 → Application → Cookies
Test 6: Session Hijacking
Copy session cookie -> Open another browser -> Replace cookie
Test 7: Session Fixation 
Note session ID before login -> Login -> Compare session ID

Using Burp Suite 
Step 1: Intercept Login Request 
• Turn Intercept ON  
• Login in browser  
Step 2: Check Response 
Go to: 
Proxy → HTTP History 
Click login request 
Step 3: Find Token 
Check: 
• Response tab  
• OR Headers:  
SetCookie:token=eyJ0eXAiOiJKVThat value = Session ID 
Result: 
Same session ID 
Inference: 
Session fixation vulnerability exists 
Test 8: Logout Mechanism 
Steps: 
1. Login → copy cookie 
2. Logout 
3. Reuse cookie 
Result: 
Session still active 
Inference: 
Session not invalidated properly 
Test 9: JWT Token Analysis 
Steps: 
1. Copy token 
2. Decode in jwt.io 
Result: 
Payload visible 
Screenshot 13: JWT decoding 
Inference: 
Improper token validation possible 

---------------------------------------------------------------------------------------------------------
